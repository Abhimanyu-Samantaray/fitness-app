spring:
  application:
    name: userservice

#  Live
#  datasource:
#    url: ${DB_URL}
#    username: ${DB_USERNAME}
#    password: ${DB_PASSWORD}
  #Local
  datasource:
    url: jdbc:postgresql://ep-bitter-math-a1k4twcv.ap-southeast-1.aws.neon.tech/fitness_users?sslmode=require
    username: neondb_owner
    password: npg_u24JtoOvMBCh

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8084/eureka/
    instance:
      prefer-ip-address: true


#For Live
#server:
#  port: ${PORT:8080}

